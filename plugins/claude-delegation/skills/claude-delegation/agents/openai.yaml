interface:
  display_name: "Claude Delegation"
  short_description: "Call Claude with schema-validated JSON output."
  default_prompt: "Use $claude-delegation to get structured JSON from Claude for:"

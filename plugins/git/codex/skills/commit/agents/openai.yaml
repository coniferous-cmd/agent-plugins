interface:
  display_name: "Git Commit"
  short_description: "Write clear commit messages from staged changes"
  default_prompt: "Use $commit to write or review a focused Git commit message from the staged changes."
